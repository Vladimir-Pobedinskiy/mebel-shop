node_modules
*.log*
.nuxt
.nitro
.cache
.output
.env
dist

# Генерируется npm — переформатировать нельзя, иначе каждый npm install даёт диff
package-lock.json

# Данные витрины: правятся руками и скриптами, отступ в два пробела.
# Прогон prettier переписал бы весь файл на табы ради нулевой пользы
db.json
